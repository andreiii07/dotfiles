eval "$(starship init zsh)"

export PATH=$PATH:/home/andreiii/.spicetify

export PATH="$HOME/.cargo/bin:$PATH"


eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv)"

pokemon-colorscripts -r 1 -s --no-title

plugins=(colorize)
